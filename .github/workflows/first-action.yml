name: First Workflow
on: workflow_dispatch
jobs: 
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "Running step 1"
          echo "...still Running step 1"
      - name: Print bye
        run: echo "Done - bye!"
